<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ltd.ygao.oneblog.mapper.ArticleMapper">
<insert id="addArticle" parameterType="ltd.ygao.oneblog.pojo.Article" keyProperty="id">
        insert into articles(id,articeName,textcontent,htmlcontent,taglist,createOn,creater,statue)
        values (#{id},#{aritcleName},#{textContent},#{htmlContent},#{tagList},#{createrOn},#{creater},statue)
    </insert>
</mapper>

